# Parse molecular formula into element counts

Parse molecular formula into element counts

## Usage

``` r
parse_formula(formula)
```

## Arguments

- formula:

  Character molecular formula (e.g. "C6H12O6")

## Value

Named numeric vector with element counts
