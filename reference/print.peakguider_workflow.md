# Print PeakGuideR workflow result

Print PeakGuideR workflow result

## Usage

``` r
# S3 method for class 'peakguider_workflow'
print(x, ...)
```

## Arguments

- x:

  A `peakguider_workflow` object.

- ...:

  Additional arguments, currently unused.

## Value

Invisibly returns `x`.
