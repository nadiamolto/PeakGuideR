% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_similarity.R
\name{preprocess_xy}
\alias{preprocess_xy}
\title{Preprocess two intensity vectors for spatial similarity scoring}
\usage{
preprocess_xy(
  x,
  y,
  min_quantile = 0.01,
  clip_negatives = TRUE,
  transform = c("none", "log1p", "zscore")
)
}
\arguments{
\item{x, y}{Numeric intensity vectors (same length).}

\item{min_quantile}{Numeric in the range 0 to 1. Feature-wise low-intensity
quantile filter applied after retaining co-detected pixels.}

\item{clip_negatives}{Logical. If \code{TRUE}, negative intensities are
truncated to zero before transformation.}

\item{transform}{Intensity transformation: \code{"none"}, \code{"log1p"} or
\code{"zscore"}.}
}
\value{
A list with \code{x} and \code{y} (preprocessed vectors), or \code{NULL} if fewer
than 3 valid pixels remain.
}
\description{
Shared internal preprocessing used by \code{\link[=adduct_candidates]{adduct_candidates()}} and
\code{\link[=build_single_adduct_candidates]{build_single_adduct_candidates()}} to prepare a pair of feature intensity
vectors before computing a spatial/intensity similarity score.
}
\keyword{internal}
