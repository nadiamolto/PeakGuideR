% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single_adduct_candidates.R
\name{build_single_adduct_candidates}
\alias{build_single_adduct_candidates}
\title{Build single-adduct candidate hypotheses for unassigned features}
\usage{
build_single_adduct_candidates(
  pkm,
  feature_summary,
  adduct_fam,
  ion_mode = c("pos", "neg"),
  matrix = NULL,
  adducts = NULL,
  compound_db = NULL,
  standards_db = NULL,
  ppm_tol = 5,
  neutral_cluster_ppm = 5,
  top_n = 10L,
  method = c("pearson", "cosine", "spearman"),
  transform = c("none", "log1p", "zscore"),
  min_quantile = 0.01,
  clip_negatives = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{pkm}{A peak matrix list with \code{mass} and \code{intensity}.}

\item{feature_summary}{Output from \code{\link[=build_feature_summary]{build_feature_summary()}}.}

\item{adduct_fam}{Output from \code{\link[=adduct_families]{adduct_families()}}.}

\item{ion_mode}{\code{"pos"} or \code{"neg"}.}

\item{matrix}{Matrix name. Standard-adduct support is applied only when
\code{matrix = "HCCA"}. Use \code{NULL} to skip it.}

\item{adducts}{Optional adduct definition data.frame. If \code{NULL},
\code{default_adducts(ion_mode)} is used.}

\item{compound_db}{Compound mass database. If \code{NULL}, the included example
database is loaded.}

\item{standards_db}{Standard adduct library. If \code{NULL} and \code{matrix = "HCCA"}, the included example library is loaded.}

\item{ppm_tol}{PPM tolerance for compound/standard mass matching.}

\item{neutral_cluster_ppm}{PPM tolerance used to cluster inferred neutral
masses, including folding hypotheses into existing adduct families.}

\item{top_n}{Maximum number of compound candidates kept per neutral mass.
Use \code{NULL} to retain all candidates within \code{ppm_tol}.}

\item{method}{Spatial similarity metric: \code{"pearson"}, \code{"cosine"} or
\code{"spearman"}.}

\item{transform}{Intensity transformation before similarity calculation:
\code{"none"}, \code{"log1p"} or \code{"zscore"}.}

\item{min_quantile}{Numeric in the range 0 to 1. Feature-wise low-intensity
quantile filter used by the spatial similarity score.}

\item{clip_negatives}{Logical. If \code{TRUE}, negative intensities are
truncated to zero before transformation.}

\item{quiet}{Logical. If \code{FALSE}, database loading functions may print
notices.}
}
\value{
A data.frame with one row per feature, inferred adduct and
candidate identity: \code{neutral_mass_id}, \code{neutral_mass_consensus},
\code{feature_idx}, \code{inferred_adduct}, \code{hypothesis_origin},
\code{adduct_spatial_score}, \code{source}, \code{identity_match_type},
\code{possible_link_formula_only}, \code{candidate_ppm_error},
\code{candidate_neutral_mass}, \verb{broad_db_*}, \verb{standard_db_*} and
\code{standard_adduct_recovery_score}.
}
\description{
Builds candidate neutral-mass hypotheses for m/z features that were not
assigned to any adduct family by \code{\link[=adduct_families]{adduct_families()}}, excluding features
already explained as isotope satellite peaks (\code{is_c13_m1}, \code{is_c13_m2},
\code{is_eips_isotope} in \code{feature_summary}).

For each unassigned feature and each adduct compatible with \code{ion_mode}, a
neutral-mass hypothesis is inferred (\code{neutral_mass = mz - adduct_mass},
the same convention as elsewhere in PeakGuideR). These hypotheses are then
clustered by ppm tolerance using the same mechanism as
\code{\link[=adduct_families]{adduct_families()}} / \code{\link[=build_neutral_mass_candidates]{build_neutral_mass_candidates()}}
(see \code{\link[=assign_neutral_mass_clusters]{assign_neutral_mass_clusters()}}), and share the \strong{same}
\code{neutral_mass_id} space as adduct families: a hypothesis that falls within
\code{neutral_cluster_ppm} of an already-detected family's neutral mass is
folded into that family's \code{neutral_mass_id} (\code{hypothesis_origin = "single_adduct_clustered"}) instead of becoming a separate entry. Isolated
hypotheses that do not fall into any existing family are clustered only
among themselves and receive new \code{neutral_mass_id} values
(\code{hypothesis_origin = "single_adduct_isolated"}).

An \code{adduct_spatial_score} is computed for every hypothesis by correlating
the feature's intensity image against every other feature already linked
to the same \code{neutral_mass_id} (family members and/or other single-adduct
hypotheses), reusing the spatial-similarity utilities in
\code{R/spatial_similarity.R}. A hypothesis with no such partner (a truly
isolated cluster of size one) gets \code{adduct_spatial_score = NA}, not \code{0}.

Candidates are then retrieved by mass from \code{compound_db}
(\code{\link[=match_compound_db_by_mass]{match_compound_db_by_mass()}}) and, when \code{matrix = "HCCA"}, from
\code{standards_db} (\code{\link[=match_standards_by_mass]{match_standards_by_mass()}}), and identity is resolved
with \code{\link[=resolve_cross_source_identity]{resolve_cross_source_identity()}} exactly as for family-derived
candidates. \code{standard_adduct_recovery_score} is computed against the
detected-adducts set enriched by the neutral-mass clustering above (family
members' adducts and every single-adduct hypothesis sharing the same
\code{neutral_mass_id}).
}
\examples{
\dontrun{
data(example_pkm, package = "PeakGuideR")

morph_results <- iso_morphology_candidates(example_pkm, prefer_mode = "ppm")
cir_results <- cir_score(morph_results, example_pkm)
eips_results <- eips_score(
  morph_results, example_pkm,
  ion_mode = "pos", cir_df = cir_results, morph_df = morph_results
)

adduct_edges <- adduct_candidates(example_pkm, ion_mode = "pos")
adduct_fam <- adduct_families(adduct_edges)

relation_table <- build_relation_table(
  cir_results = cir_results,
  eips_results = eips_results,
  adduct_fam = adduct_fam
)
feature_summary <- build_feature_summary(
  relation_table = relation_table,
  adduct_fam = adduct_fam,
  pkm = example_pkm
)

single_adduct_hyp <- build_single_adduct_candidates(
  pkm = example_pkm,
  feature_summary = feature_summary,
  adduct_fam = adduct_fam,
  ion_mode = "pos",
  matrix = "HCCA"
)
head(single_adduct_hyp)
}
}
