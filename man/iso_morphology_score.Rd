% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Isotope-core.R
\name{iso_morphology_score}
\alias{iso_morphology_score}
\title{Isotope morphology score using binSize-aware tolerance (preferred for binned data)}
\usage{
iso_morphology_score(
  pm,
  prefer_mode = c("binsize", "dp", "ppm"),
  N_bins = 3L,
  tol_dp = 3L,
  tol_ppm = 5,
  max_iso = 2L,
  charges = 1L,
  include_heavy_M2 = TRUE,
  require_contiguity = TRUE,
  min_pixels = 20L,
  use_intercept = TRUE,
  combine = c("min", "mean", "weighted"),
  weights = NULL
)
}
\arguments{
\item{pm}{list with fields:
\itemize{
\item mass: numeric vector of length p (feature m/z centers).
\item intensity: numeric matrix \link{Pixels x p}.
\item binSize: (optional) numeric vector of length p (bin width in Da per feature).
}}

\item{prefer_mode}{character: "binsize" (default), "dp", or "ppm".
If "binsize" is chosen but binSize is missing, it falls back to "dp" (then "ppm").}

\item{N_bins}{integer: multiplier for binSize tolerance (± N_bins * binSize) when using binsize mode.}

\item{tol_dp}{integer: ± data points window (when using dp mode).}

\item{tol_ppm}{numeric: ppm window (when using ppm mode).}

\item{max_iso}{integer: how many isotope steps (k) to test (1 = M+1; 2 = M+1 & M+2; ...).}

\item{charges}{integer vector: charge states to consider (e.g., c(1L,2L)).}

\item{include_heavy_M2}{logical: if TRUE, also test ~2 Da M+2 shifts (Cl/Br/S).}

\item{require_contiguity}{logical: if TRUE, only accept M+2 if M+1 was valid (non-NA R^2).}

\item{min_pixels}{integer: minimum pixels used in regression after paired-zero filtering.}

\item{use_intercept}{logical: TRUE → lm(y ~ x); FALSE → lm(y ~ 0 + x).}

\item{combine}{"min"|"mean"|"weighted": how to combine per-step R^2 into final morphology score.}

\item{weights}{numeric: weights for k=1..max_iso if combine="weighted".}
}
\value{
data.frame with per-M0 results:
idx_M0, mz_M0, score_morph, and for k=1..max_iso: idx_Mk, mz_Mk, r2_Mk
}
\description{
Isotope morphology score using binSize-aware tolerance (preferred for binned data)
}
