% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ion_image.R
\name{plot_ion_image}
\alias{plot_ion_image}
\title{Plot an ion image}
\usage{
plot_ion_image(
  pkm,
  idx = NULL,
  mz = NULL,
  tol_ppm = 10,
  palette = "viridis",
  title = NULL,
  flip_y = TRUE,
  clip_quantile = NULL,
  show_legend = TRUE
)
}
\arguments{
\item{pkm}{Peak matrix list containing at least \code{mass} (numeric vector),
\code{intensity} (numeric pixel x feature matrix) and \code{pos} (matrix or
data.frame with \code{x}, \code{y} pixel coordinate columns).}

\item{idx}{Optional integer. Column index of the feature to plot in
\code{pkm$mass}/\code{pkm$intensity} (the same indexing convention used by
\code{feature_summary$idx}, \code{morph_results$idx_M0}/\code{idx_cand},
\code{adduct_edges$idx_i}/\code{idx_j}, \code{candidate_annotations$feature_idx} and
\code{adduct_fam$family_members$idx}). This is the preferred way to select a
feature. Exactly one of \code{idx} or \code{mz} must be supplied.}

\item{mz}{Optional numeric. Target m/z used to look up the closest feature
in \code{pkm$mass} within \code{tol_ppm}, for exploratory use when a feature index
is not already known. Exactly one of \code{idx} or \code{mz} must be supplied.}

\item{tol_ppm}{PPM tolerance used when searching by \code{mz}. Ignored (without
a warning) when \code{idx} is supplied.}

\item{palette}{Colour palette for the intensity scale. \code{"viridis"}
(default) uses the blue-to-yellow \code{ggplot2::scale_fill_viridis_c()}
scale. Any other value is passed through as the \code{option} argument of
\code{scale_fill_viridis_c()} (e.g. \code{"magma"}, \code{"inferno"}).}

\item{title}{Optional plot title. If \code{NULL} (default), a title is
generated from the plotted feature's m/z - when the feature was located
via \code{mz}, the title reflects the m/z actually found, not the one
requested.}

\item{flip_y}{Logical. If \code{TRUE} (default), the pixel y coordinate is
negated before plotting, matching the top-left image origin convention
used elsewhere in the package.}

\item{clip_quantile}{Optional numeric in \verb{(0, 1]}. If supplied, intensity
values above this quantile are clipped before mapping to colour, so a
handful of saturated pixels do not flatten the rest of the colour scale.}

\item{show_legend}{Logical. If \code{FALSE}, the intensity colour legend is
omitted.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the spatial ion image (per-pixel intensity) of a single feature in a
peak matrix, using \code{ggplot2}.
}
\examples{
\dontrun{
data(example_pkm, package = "PeakGuideR")

plot_ion_image(example_pkm, idx = 1)
plot_ion_image(example_pkm, mz = 762.6, tol_ppm = 50)
}

}
