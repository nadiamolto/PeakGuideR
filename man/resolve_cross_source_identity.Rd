% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_identity.R
\name{resolve_cross_source_identity}
\alias{resolve_cross_source_identity}
\title{Resolve compound identity across a broad compound database and the
standard-adduct library}
\usage{
resolve_cross_source_identity(compound_candidates, standard_candidates = NULL)
}
\arguments{
\item{compound_candidates}{A data.frame of \code{compound_db}-derived
candidates with at least \code{neutral_mass_id}, \code{candidate_source},
\code{candidate_db_id}, \code{candidate_name}, \code{candidate_formula},
\code{candidate_neutral_mass}, \code{candidate_ppm_error}, \code{candidate_inchi},
\code{candidate_inchikey}, \code{candidate_smiles}.}

\item{standard_candidates}{A data.frame of \code{standards_db}-derived
candidates, typically the output of \code{\link[=match_standards_by_mass]{match_standards_by_mass()}}, or
\code{NULL}/empty if standard-adduct support is not being used.}
}
\value{
A data.frame with one row per resolved candidate identity and
columns: \code{neutral_mass_id}, \code{source} (\code{"both"}, \code{"broad_db_only"} or
\code{"standards_only"}), \code{identity_match_type}, \code{possible_link_formula_only},
\code{candidate_ppm_error}, \code{candidate_neutral_mass}, \code{broad_db_name},
\code{broad_db_formula}, \code{broad_db_inchikey}, \code{broad_db_inchi},
\code{broad_db_smiles}, \code{broad_db_id}, \code{broad_db_source},
\code{standard_db_name}, \code{standard_db_formula}, \code{standard_db_inchikey},
\code{standard_db_smiles}, \code{standard_db_compound_id}, \code{standard_db_adducts}.
}
\description{
Within each \code{neutral_mass_id}, links \code{compound_db}-derived candidates
(\code{compound_candidates}) with \code{standards_db}-derived candidates
(\code{standard_candidates}, typically from \code{\link[=match_standards_by_mass]{match_standards_by_mass()}}) using
a hierarchy of identifiers, stopping at the first level that produces a
match: \code{InChIKey} -> \code{InChI} -> \code{SMILES} -> shared database ID (\code{HMDB} or
\code{ChEBI}, only when the \code{compound_db} row's \code{Source} matches that identifier
type). Matching is 1:1: a given candidate is linked to at most one
counterpart, using the strongest available identifier level.

A match on molecular formula alone (with or without an exact name match)
is \strong{never} treated as identity. Such rows are kept as separate
\code{"broad_db_only"} / \code{"standards_only"} rows and cross-referenced through
\code{possible_link_formula_only} for manual review.

\code{identity_match_type} is bookkeeping only: it records which identifier
level produced a \code{"both"} fusion, but must not be used as a numeric
evidence score.
}
\examples{
# "Citric acid" and "Isocitric acid" share the formula C6H8O7, but only
# "Citric acid" shares a ChEBI id with the standard "CITRATE": formula
# alone must not fuse the two compounds.
compound_candidates <- data.frame(
  neutral_mass_id = c(1L, 1L),
  candidate_source = c("CHEBI", "CHEBI"),
  candidate_db_id = c("30769", "30887"),
  candidate_name = c("Citric acid", "Isocitric acid"),
  candidate_formula = c("C6H8O7", "C6H8O7"),
  candidate_neutral_mass = c(192.027, 192.027),
  candidate_ppm_error = c(0, 0),
  candidate_inchi = NA_character_,
  candidate_inchikey = NA_character_,
  candidate_smiles = NA_character_,
  stringsAsFactors = FALSE
)

standard_candidates <- data.frame(
  neutral_mass_id = 1L,
  candidate_source = "standards_db",
  candidate_name = "CITRATE",
  candidate_formula = "C6H8O7",
  candidate_neutral_mass = 192.027,
  candidate_ppm_error = 0,
  candidate_db_id = "P1A5",
  candidate_inchikey = "KRKNYBCHXYNGOX-UHFFFAOYSA-N",
  candidate_smiles = "OC(=O)CC(O)(CC(O)=O)C(O)=O",
  candidate_hmdb = "HMDB0000094",
  candidate_chebi = "30769",
  standard_db_adducts = "[M-H]-",
  stringsAsFactors = FALSE
)

resolve_cross_source_identity(compound_candidates, standard_candidates)

}
