% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_identity.R
\name{resolve_identity_key}
\alias{resolve_identity_key}
\title{Resolve a per-row chemical identity key for isomer-ambiguity detection}
\usage{
resolve_identity_key(combined)
}
\arguments{
\item{combined}{A data.frame with \code{broad_db_inchikey}, \code{broad_db_inchi},
\code{broad_db_smiles}, \code{broad_db_id}, \code{broad_db_name}, \code{broad_db_formula},
\code{standard_db_inchikey}, \code{standard_db_smiles}, \code{standard_db_compound_id},
\code{standard_db_name}, \code{standard_db_formula} columns.}
}
\value{
A character vector, the same length as \code{nrow(combined)}, with one
identity key per row.
}
\description{
Collapses candidate rows that refer to the same chemical identity before
\code{ambiguous_isomeric} is derived from their \code{priority_score}s, so that two
rows describing the \emph{same} compound never look like two competing
candidates. This matters most for \code{hypothesis_origin == "single_adduct_isolated"} rows: two adducts of one compound that were
each inferred independently (because they never merged into a shared
adduct family) otherwise produce two rows with an identical
\code{priority_score}, which a naive top-row-vs-second-row comparison flags as
ambiguous even though there is no second identity at all.

Uses the same identifier hierarchy as \code{\link[=resolve_cross_source_identity]{resolve_cross_source_identity()}}:
\code{InChIKey} -> \code{InChI} -> \code{SMILES} -> database id (\code{broad_db_id} /
\code{standard_db_compound_id}) -> name + formula, stopping at the first
non-missing identifier (falling back from the \verb{broad_db_*} to the
\verb{standard_db_*} columns at each level, since a row can carry either or
both depending on \code{source}). Rows with none of these populated (no
resolved database candidate at all) each receive their own unique key, so
they are never collapsed with one another.
}
\keyword{internal}
