% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_adduct_family.R
\name{plot_adduct_family}
\alias{plot_adduct_family}
\title{Plot all members of an adduct family}
\usage{
plot_adduct_family(pkm, family_id, adduct_fam, ncol = NULL, ...)
}
\arguments{
\item{pkm}{Peak matrix list, see \code{plot_ion_image()}.}

\item{family_id}{Adduct family ID to plot (matches
\code{adduct_fam$family_members$family_id} /
\code{adduct_fam$family_summary$family_id}).}

\item{adduct_fam}{List returned by \code{adduct_families()}, containing
\code{family_members} and \code{family_summary}.}

\item{ncol}{Optional integer. Number of columns used to arrange the
panels. If \code{NULL} (default), a reasonable number of columns is chosen
automatically from the family size (\code{ceiling(sqrt(n))}).}

\item{...}{Additional arguments passed through to \code{plot_ion_image()} (e.g.
\code{palette}, \code{clip_quantile}, \code{flip_y}, \code{show_legend}).}
}
\value{
A \code{patchwork} object combining one ion image panel per family
member.
}
\description{
Plots the ion image of every feature belonging to a given adduct family
(as returned by \code{adduct_families()}), reusing \code{plot_ion_image()} for each
panel and combining them with \code{patchwork}.
}
\examples{
\dontrun{
data(example_pkm, package = "PeakGuideR")

adduct_res <- adduct_candidates(example_pkm, ion_mode = "pos")
adduct_fam <- adduct_families(adduct_res)

plot_adduct_family(
  example_pkm,
  family_id = adduct_fam$family_summary$family_id[1],
  adduct_fam = adduct_fam
)
}

}
