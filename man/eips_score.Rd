% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eips_score.R
\name{eips_score}
\alias{eips_score}
\title{Score elemental isotope-pattern candidates}
\usage{
eips_score(
  result,
  pkm,
  eips_n_table = NULL,
  eips_table = NULL,
  ion_mode = c("pos", "neg"),
  adducts = NULL,
  min_score_final = 0.6,
  eips_rel_tol = 0.3,
  ratio_method = c("sum", "median", "mean"),
  min_quantile = 0.01,
  ppm_neutral = 5,
  n_window = 1L,
  top_n_hat = 5L,
  cir_df = NULL,
  morph_df = NULL,
  require_c13_for = c("N", "O", "S"),
  exclude_c13_m2 = TRUE,
  return_debug = FALSE
)
}
\arguments{
\item{result}{Output of \code{iso_morphology_candidates()} (must contain at least
\code{idx_M0}, \code{idx_cand}, \code{score_final}, \code{element}, \code{iso_type}).}

\item{pkm}{Peak matrix list with \code{mass} and \code{intensity}.}

\item{eips_n_table}{Optional precomputed theoretical lookup table with
columns \code{element}, \code{k}, \code{delta}, \code{n} and \code{R_theo}. If \code{NULL}, the internal
PeakGuideR \code{eips_n_table} object is used.}

\item{eips_table}{Optional database-derived lookup table with columns
\code{mz_mono}, \code{element} and \code{n_el}. If \code{NULL}, the internal PeakGuideR
\code{eips_table} object is used.}

\item{ion_mode}{\code{"pos"} or \code{"neg"} (used only when formula support is enabled).}

\item{adducts}{Optional data.frame with adduct definitions. If \code{NULL},
\code{default_adducts()} is used. The table must contain \code{name}, \code{mode} and
\code{mass}, where \code{mass} is the net m/z shift relative to the neutral mass.}

\item{min_score_final}{Minimum morphology score required to consider a pair.}

\item{eips_rel_tol}{Relative tolerance used as validity threshold and for
ratio score scaling.}

\item{ratio_method}{\code{"sum"}, \code{"mean"}, or \code{"median"} aggregation of isotope ratios.
\code{"sum"} computes sum(I_iso) / sum(I_M0) over selected pixels and is recommended
for MSI isotope-ratio estimation.}

\item{min_quantile}{Quantile used for intensity masking. Default= 0.01.}

\item{ppm_neutral}{Ppm tolerance used when evaluating neutral mass
consistency for elemental isotope-pattern support.}

\item{n_window}{Allowed deviation between inferred \code{n_hat} and formula \code{n_el}
when formula support is evaluated.}

\item{top_n_hat}{Integer. Keep top-k best \code{n_hat} candidates by relative error.}

\item{cir_df}{Optional CIR results table. Must contain \code{idx_M0} and
\code{is_valid_c13}. If supplied, enables the N/O/S gate.}

\item{morph_df}{Optional morphology table (typically the same object passed in
\code{result}). Must contain \code{idx_M0}, \code{idx_cand}, \code{iso_type}. If supplied,
enables exclusion of C13_M2 pairs.}

\item{require_c13_for}{Character vector of elements for which CIR-validated
C13_M1 is required. Default: \code{c("N","O","S")}.}

\item{exclude_c13_m2}{Logical. If \code{TRUE}, excludes pairs already detected as
C13_M2 for \code{idx_M0} values with CIR-valid C13_M1.}

\item{return_debug}{Logical. If \code{TRUE}, returns a list with final output and
internal top-n candidates.}
}
\value{
By default, a single \code{data.frame} / tibble with one row per evaluated pair:
\itemize{
\item \code{idx_M0}, \code{mz_M0}, \code{idx_cand}, \code{mz_iso}
\item \code{iso_type}, \code{element}, \code{k}, \code{delta}
\item \code{R_obs}, \code{n_hat}, \code{R_theo_hat}, \code{eips_rel_err}
\item \code{is_valid_eips}, \code{score_eips}
\item \code{has_formula_support}, \code{adduct_name}, \code{neutral_mass},
\code{mz_mono_db}, \code{n_el_db}, \code{neutral_err_ppm}
}

If \code{return_debug = TRUE}, returns a list with:
\itemize{
\item \code{eips_validation}
\item \code{top_n_candidates}
}
}
\description{
Evaluates non-carbon isotope-pattern candidates by comparing the observed
isotope ratio against theoretical element-count ratios for N, O, S, Cl and Br.

The function can optionally add formula support by comparing inferred neutral
masses with a database-derived EIPS lookup table.

Two optional reliability gates can be applied:
\itemize{
\item For N/O/S, EIPS is only evaluated if the same \code{idx_M0} has a
CIR-validated C13_M1 isotope.
\item EIPS is not evaluated for pairs already detected as C13_M2 for an
\code{idx_M0} with CIR-validated C13_M1.
}

For low-abundance N/O/S isotope channels, the function can require independent
C13 support for the same monoisotopic feature. This reduces spurious elemental
isotope assignments in noisy MSI peak tables.

In addition, inferred atom counts (\code{n_hat}) are bounded by the maximum number
of atoms observed in the database for that element. Candidates exceeding that
bound are discarded before final selection.

Expected isotope peak order for the elemental channel evaluated here.
N is evaluated at M+1; O, S, Cl and Br are evaluated at M+2.

EIPS does not perform the initial mass-difference search. It evaluates
elemental isotope candidates already detected by \code{iso_morphology_candidates()}.
}
