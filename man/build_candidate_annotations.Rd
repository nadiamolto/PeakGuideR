% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_candidate_annotations.R
\name{build_candidate_annotations}
\alias{build_candidate_annotations}
\title{Build the unified long-format candidate annotation table}
\usage{
build_candidate_annotations(
  adduct_fam,
  feature_summary,
  pkm = NULL,
  ion_mode = c("pos", "neg"),
  matrix = NULL,
  adducts = NULL,
  compound_db = NULL,
  standards_db = NULL,
  ppm_tol = 5,
  neutral_cluster_ppm = 5,
  top_n = 10L,
  weights = default_priority_weights(),
  recovery_threshold = 0.5,
  adduct_min_score = 0.5,
  ambiguity_gap = 0.05,
  include_single_adduct = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{adduct_fam}{Output from \code{\link[=adduct_families]{adduct_families()}}.}

\item{feature_summary}{Output from \code{\link[=build_feature_summary]{build_feature_summary()}}.}

\item{pkm}{Optional peak matrix (\code{mass}, \code{intensity}). Required to include
single-adduct hypotheses (see \code{include_single_adduct}); if \code{NULL}, the
table contains family-derived candidates only.}

\item{ion_mode}{\code{"pos"} or \code{"neg"}.}

\item{matrix}{Matrix name. Standard-adduct support is applied only when
\code{matrix = "HCCA"}. Use \code{NULL} to skip it.}

\item{adducts}{Optional adduct definition data.frame, used only for
single-adduct hypotheses. If \code{NULL}, \code{default_adducts(ion_mode)} is
used.}

\item{compound_db}{Compound mass database. If \code{NULL}, the included example
database is loaded.}

\item{standards_db}{Standard adduct library. If \code{NULL} and \code{matrix = "HCCA"}, the included example library is loaded.}

\item{ppm_tol}{PPM tolerance for compound/standard mass matching.}

\item{neutral_cluster_ppm}{PPM tolerance used to cluster inferred neutral
masses.}

\item{top_n}{Maximum number of compound candidates kept per neutral mass.
Use \code{NULL} to retain all candidates within \code{ppm_tol}.}

\item{weights}{Named numeric vector of weights for \code{priority_score}; see
\code{\link[=default_priority_weights]{default_priority_weights()}}.}

\item{recovery_threshold}{Minimum \code{standard_adduct_recovery_score} (\code{NA}
treated as \code{0}) required for \code{confidence_class = "cross_db_linked_adduct_recovered"} when \code{source == "both"}.}

\item{adduct_min_score}{Minimum \code{adduct_spatial_score} (\code{NA} treated as
\code{0}) counted as multi-evidence support for \code{broad_db_only} candidates.
Reuses the same threshold as the \code{adduct_min_score} workflow parameter.}

\item{ambiguity_gap}{Minimum \code{priority_score} gap between the top-1 and
top-2 candidate of the same \code{neutral_mass_id} required to \emph{not} flag
\code{ambiguous_isomeric}.}

\item{include_single_adduct}{Logical. If \code{FALSE}, or if \code{pkm} is \code{NULL},
single-adduct hypotheses are not computed.}

\item{quiet}{Logical. If \code{FALSE}, database loading functions may print
notices.}
}
\value{
A data.frame with one row per neutral mass, feature(s) and
candidate identity, including \code{neutral_mass_id},
\code{neutral_mass_consensus}, \code{feature_idx}, \code{inferred_adduct}, \code{source},
\code{identity_match_type}, \code{possible_link_formula_only},
\code{hypothesis_origin}, \verb{broad_db_*}, \verb{standard_db_*},
\code{mass_error_score}, \code{adduct_spatial_score}, \code{isotope_evidence_score},
\code{eips_evidence_score}, \code{standard_adduct_recovery_score},
\code{family_coherence_score}, \code{priority_score}, \code{confidence_class} and
\code{ambiguous_isomeric}.

\code{confidence_class} summarizes the evidence combination behind each
candidate into one of six priority-ordered levels:
\itemize{
\item \code{"cross_db_linked_adduct_recovered"}: \code{source == "both"} (the
broad-database and standard-library identity were linked through a
strong cross-database identifier) and \code{standard_adduct_recovery_score}
(\code{NA} treated as \code{0}) is at least \code{recovery_threshold}. This reflects a
cross-database identifier link plus recovered adduct evidence, not an
analytical confirmation of the compound in the sample.
\item \code{"cross_db_linked_partial_recovery"}: \code{source == "both"}, but the
\code{recovery_threshold} above is not met.
\item \code{"standards_only"}: \code{source == "standards_only"}.
\item \code{"broad_db_only_multi_evidence"}: \code{source == "broad_db_only"} and
at least two of \code{is_c13_m0}, \code{has_eips},
\code{adduct_spatial_score >= adduct_min_score} (\code{NA} treated as \code{0}) hold.
\item \code{"broad_db_only_single_evidence"}: \code{source == "broad_db_only"} and
exactly one of those three signals holds.
\item \code{"broad_db_only_mass_only"}: \code{source == "broad_db_only"} and none
of those three signals hold.
}
}
\description{
Builds one long candidate-annotation table combining adduct-family
evidence and single-adduct hypotheses (see
\code{\link[=build_single_adduct_candidates]{build_single_adduct_candidates()}}), with database and standard-adduct
identity resolved via \code{\link[=resolve_cross_source_identity]{resolve_cross_source_identity()}} instead of the
identifier-hierarchy-free join used previously. This is the table returned
as \code{candidate_annotations} by \code{\link[=run_peakguider_workflow]{run_peakguider_workflow()}}.
\code{\link[=build_neutral_mass_candidates]{build_neutral_mass_candidates()}} is a per-\code{neutral_mass_id} summary view
derived from this table.

Each row represents one neutral mass, candidate identity (from
\code{compound_db}, \code{standards_db}, or both fused via a strong identifier
match) and evidence source. A match on molecular formula alone is never
fused into a single identity; see \code{\link[=resolve_cross_source_identity]{resolve_cross_source_identity()}}.
}
\examples{
\dontrun{
data(example_pkm, package = "PeakGuideR")

morph_results <- iso_morphology_candidates(example_pkm, prefer_mode = "ppm")
cir_results <- cir_score(morph_results, example_pkm)
eips_results <- eips_score(
  morph_results, example_pkm,
  ion_mode = "pos", cir_df = cir_results, morph_df = morph_results
)

adduct_edges <- adduct_candidates(example_pkm, ion_mode = "pos")
adduct_fam <- adduct_families(adduct_edges)

relation_table <- build_relation_table(
  cir_results = cir_results,
  eips_results = eips_results,
  adduct_fam = adduct_fam
)
feature_summary <- build_feature_summary(
  relation_table = relation_table,
  adduct_fam = adduct_fam,
  pkm = example_pkm
)

candidate_annotations <- build_candidate_annotations(
  adduct_fam = adduct_fam,
  feature_summary = feature_summary,
  pkm = example_pkm,
  ion_mode = "pos",
  matrix = "HCCA"
)
head(candidate_annotations)
}
}
