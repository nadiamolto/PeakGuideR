% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_isotope_pair.R
\name{plot_isotope_pair}
\alias{plot_isotope_pair}
\title{Plot an isotope pair (and, when available, its M+2 satellite)}
\usage{
plot_isotope_pair(
  pkm,
  idx_M0,
  idx_cand,
  morph_row = NULL,
  cir_row = NULL,
  eips_row = NULL,
  ...
)
}
\arguments{
\item{pkm}{Peak matrix list, see \code{plot_ion_image()}.}

\item{idx_M0}{Integer. Column index of the monoisotopic (M0) feature.}

\item{idx_cand}{Integer. Column index of the candidate isotope satellite
feature (M+k).}

\item{morph_row}{Optional single-row data.frame - one row of the output of
\code{iso_morphology_candidates()} - used (if \code{cir_row} and \code{eips_row} are not
supplied) to annotate the plot subtitle with \code{score_final}.}

\item{cir_row}{Optional single-row data.frame - one row of the output of
\code{cir_score()}. When supplied, its \code{R_obs}/\code{R_theo}/\code{cir_class} are used
for the plot subtitle (taking priority over \code{eips_row}/\code{morph_row}), and
an M+2 panel is added automatically when \code{has_C13_M2} is \code{TRUE}.}

\item{eips_row}{Optional single-row data.frame - one row of the output of
\code{eips_score()}. Used for the plot subtitle when \code{cir_row} is not
supplied.}

\item{...}{Additional arguments passed through to \code{plot_ion_image()} (e.g.
\code{palette}, \code{clip_quantile}, \code{flip_y}, \code{show_legend}).}
}
\value{
A \code{patchwork} object combining the M0/M+k (and, when available,
M+2) ion image panels.
}
\description{
Plots the ion image of a candidate monoisotopic feature (M0) side by side
with its candidate isotope satellite (M+k), reusing \code{plot_ion_image()} for
each panel. If a \code{cir_score()} row is supplied and it has a supported M+2
satellite (\code{has_C13_M2 == TRUE}), a third panel for that M+2 feature is
added automatically.
}
\examples{
\dontrun{
data(example_pkm, package = "PeakGuideR")

morph_results <- iso_morphology_candidates(example_pkm, prefer_mode = "ppm")
cir_results <- cir_score(morph_results, example_pkm)

one_row <- cir_results[1, ]
plot_isotope_pair(
  example_pkm,
  idx_M0 = one_row$idx_M0,
  idx_cand = one_row$idx_M1,
  cir_row = one_row
)
}

}
