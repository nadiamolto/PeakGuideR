% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_identity.R
\name{compute_standard_adduct_recovery}
\alias{compute_standard_adduct_recovery}
\title{Standard-adduct recovery score for a single candidate row}
\usage{
compute_standard_adduct_recovery(standard_db_adducts, detected_adducts)
}
\arguments{
\item{standard_db_adducts}{Character scalar: semicolon-separated adducts
annotated in \code{standards_db} for this candidate compound, or \code{NA} if this
candidate has no standards-library link.}

\item{detected_adducts}{Character vector of adducts detected
computationally for the same \code{neutral_mass_id}.}
}
\value{
A single numeric value in the range 0 to 1, or \code{NA_real_}.
}
\description{
Computes, for one standard-linked candidate compound, the fraction of its
\code{standards_db}-annotated adducts that were also detected computationally
for the same neutral mass:
\deqn{score = |detected \cap standard| / |standard|}

Returns \code{NA} (not \code{0}) when there is no standard-library compound within
tolerance for that neutral mass, since absence of an experimental
reference is not negative evidence.

When several standard-library compounds (isomers) fall within tolerance of
the same neutral mass, each is scored independently against the same
\code{detected_adducts}, which can help discriminate between isomers based on
which one's expected adducts were actually observed.
}
\keyword{internal}
