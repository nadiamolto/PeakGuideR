% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_peakguider_databases.R
\name{deduplicate_compound_identity}
\alias{deduplicate_compound_identity}
\title{Deduplicate a compound mass database by resolved compound identity}
\usage{
deduplicate_compound_identity(compound_db)
}
\arguments{
\item{compound_db}{A compound mass database data.frame. If it does not
already contain a \code{compound_identity_id} column,
\code{\link[=resolve_compound_identity]{resolve_compound_identity()}} is applied first.}
}
\value{
A deduplicated data.frame with one row per \code{compound_identity_id}.
}
\description{
Collapses \code{compound_db} to one representative row per
\code{compound_identity_id} (see \code{\link[=resolve_compound_identity]{resolve_compound_identity()}}), preferring the
row with the most complete identifiers when several rows share the same
identity (for example the same compound reported under \code{Source == "HMDB"}
and \code{Source == "NORMAN"}).
}
\keyword{internal}
