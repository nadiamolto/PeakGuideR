% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_identity.R
\name{match_standards_by_mass}
\alias{match_standards_by_mass}
\title{Match inferred neutral masses against the standard-adduct library by mass}
\usage{
match_standards_by_mass(
  neutral_masses,
  standards_db,
  ion_mode = c("pos", "neg"),
  matrix = NULL,
  ppm_tol = 5
)
}
\arguments{
\item{neutral_masses}{A data.frame with at least \code{neutral_mass_id} and
\code{neutral_mass_consensus}.}

\item{standards_db}{Standard-adduct library data.frame. Must contain
\code{NEUTRAL_MONOISOTOPIC_MASS} and \code{POLARITY}.}

\item{ion_mode}{\code{"pos"} or \code{"neg"}.}

\item{matrix}{Optional matrix name. When supplied and \code{standards_db}
contains a \code{matrix} column, only rows whose \code{matrix} value contains
\code{matrix} (case-insensitive) are kept. Use \code{NULL} to skip matrix
filtering.}

\item{ppm_tol}{PPM tolerance for neutral-mass matching.}
}
\value{
A data.frame with one row per \code{neutral_mass_id} and standard
compound candidate, with columns \code{neutral_mass_id}, \code{candidate_source}
(\code{"standards_db"}), \code{candidate_name}, \code{candidate_formula},
\code{candidate_neutral_mass}, \code{candidate_ppm_error}, \code{candidate_db_id},
\code{candidate_inchikey}, \code{candidate_smiles}, \code{candidate_hmdb},
\code{candidate_chebi} and \code{standard_db_adducts} (semicolon-separated adducts
annotated for that compound in \code{standards_db}, after ion-mode/matrix
filtering).
}
\description{
Retrieves standard-adduct library compounds within \code{ppm_tol} of each
inferred neutral mass, symmetrically to how \code{compound_db} is matched by
mass elsewhere in PeakGuideR.

Unlike the previous implementation, this function never joins on
identifiers hanging off a \code{compound_db} candidate row: \code{standards_db} is
treated as an independent evidence source that is retrieved purely by
neutral mass, ion mode and (optionally) matrix. Cross-source identity is
resolved afterwards by \code{\link[=resolve_cross_source_identity]{resolve_cross_source_identity()}}.

Because \code{standards_db} typically has several adduct rows per compound
(all sharing the same \code{NEUTRAL_MONOISOTOPIC_MASS}), matches are collapsed
to one row per standard compound and per \code{neutral_mass_id}, with
\code{standard_db_adducts} listing every annotated adduct for that compound
(used downstream to compute \code{standard_adduct_recovery_score}). When two or
more standard-library compounds (isomers) fall within tolerance of the
same neutral mass, they are kept as separate rows rather than merged.
}
\examples{
standards_db <- load_standards_adduct_library(quiet = TRUE)

neutral_masses <- data.frame(
  neutral_mass_id = 1L,
  neutral_mass_consensus = standards_db$NEUTRAL_MONOISOTOPIC_MASS[1]
)

match_standards_by_mass(
  neutral_masses,
  standards_db,
  ion_mode = "neg",
  matrix = "HCCA"
)

}
