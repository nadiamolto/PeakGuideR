% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adduct_group.R
\name{adduct_families}
\alias{adduct_families}
\title{Build neutral-mass-based adduct families from pairwise adduct candidate edges}
\usage{
adduct_families(
  adduct_edges,
  min_score_adduct = 0.3,
  neutral_cluster_ppm = 5,
  min_family_size = 2L,
  use_only_valid = TRUE
)
}
\arguments{
\item{adduct_edges}{A \code{data.frame} produced by \code{adduct_candidates()}.
Must contain at least:
\code{idx_i}, \code{idx_j}, \code{mz_i}, \code{mz_j}, \code{score_adduct},
\code{neutral_mass_i}, \code{neutral_mass_j}, \code{neutral_mass_mean},
\code{adduct_i}, \code{adduct_j}, \code{is_valid_adduct}.}

\item{min_score_adduct}{Numeric in the range 0 to 1. Minimum adduct score
required to include an edge. Default \code{0.2}.}

\item{neutral_cluster_ppm}{Numeric. PPM tolerance used to cluster edges into
neutral-mass families. Default \code{5}.}

\item{min_family_size}{Integer. Minimum number of unique features required to
keep a family. Default \code{2L}.}

\item{use_only_valid}{Logical. If \code{TRUE}, only edges with
\code{is_valid_adduct == TRUE} are used. Default \code{TRUE}.}
}
\value{
A list with:
\itemize{
\item \code{family_summary}: one row per neutral-mass adduct family
\item \code{family_members}: one row per feature/adduct role within a family
\item \code{family_edges}: one row per adduct edge assigned to a family
}

\code{family_summary} contains:
\itemize{
\item \code{family_id}
\item \code{neutral_mass_consensus}
\item \code{neutral_mass_range_ppm}
\item \code{family_size}
\item \code{n_edges}
\item \code{mean_score_adduct}
\item \code{median_score_adduct}
\item \code{adducts}
\item \code{feature_idx}
\item \code{has_role_conflict}
}

\code{family_members} contains:
\itemize{
\item \code{family_id}
\item \code{idx}
\item \code{mz}
\item \code{adduct}
\item \code{neutral_mass_from_feature}
\item \code{n_edges}
\item \code{mean_edge_score}
}

\code{family_edges} contains the filtered adduct candidate edges plus:
\itemize{
\item \code{family_id}
}
}
\description{
Groups adduct candidate edges into putative adduct families using the pairwise
output of \code{adduct_candidates()}. In contrast to simple graph-based grouping,
this function groups edges by their inferred neutral mass, so that alternative
adduct hypotheses involving the same feature are kept as separate neutral-mass
families rather than being forced into the same connected component.

The function returns:
\itemize{
\item a family-level summary table (\code{family_summary})
\item a feature-level membership table (\code{family_members})
\item an edge-level table with family assignment (\code{family_edges})
}

This is intended to provide chemically more interpretable adduct families,
where each family represents a candidate neutral mass supported by one or more
mass-compatible and spatially correlated adduct relationships.
}
\examples{
\dontrun{
adduct_res <- adduct_candidates(pkm, ion_mode = "pos")

adduct_fam <- adduct_families(
  adduct_res,
  min_score_adduct = 0.3,
  neutral_cluster_ppm = 5
)

head(adduct_fam$family_summary)
head(adduct_fam$family_members)
head(adduct_fam$family_edges)
}
}
