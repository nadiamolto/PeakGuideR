% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_identity.R
\name{expand_compound_candidates_for_standards}
\alias{expand_compound_candidates_for_standards}
\title{Recover compound_db candidates that top_n truncated away from a
standard-linked neutral mass}
\usage{
expand_compound_candidates_for_standards(
  compound_candidates,
  standard_candidates,
  neutral_for_matching,
  compound_db,
  ppm_tol
)
}
\arguments{
\item{compound_candidates}{Output of \code{\link[=match_compound_db_by_mass]{match_compound_db_by_mass()}},
possibly truncated by \code{top_n}.}

\item{standard_candidates}{Output of \code{\link[=match_standards_by_mass]{match_standards_by_mass()}}, or
\code{NULL}/empty if standard-adduct support is not being used.}

\item{neutral_for_matching}{The same data.frame (\code{neutral_mass_id},
\code{neutral_mass_consensus}) passed to \code{\link[=match_compound_db_by_mass]{match_compound_db_by_mass()}} to
produce \code{compound_candidates}.}

\item{compound_db}{The same \code{compound_db} passed to
\code{\link[=match_compound_db_by_mass]{match_compound_db_by_mass()}}.}

\item{ppm_tol}{The same \code{ppm_tol} passed to \code{\link[=match_compound_db_by_mass]{match_compound_db_by_mass()}}.}
}
\value{
\code{compound_candidates}, with any \code{compound_db} candidates that
\code{top_n} had excluded for a standard-linked \code{neutral_mass_id} added
back in.
}
\description{
\code{top_n} in \code{\link[=match_compound_db_by_mass]{match_compound_db_by_mass()}} is a blind cut by ascending
\code{candidate_ppm_error}: it has no visibility into \code{standards_db}, so a
candidate that would otherwise resolve to \code{source == "both"} in
\code{\link[=resolve_cross_source_identity]{resolve_cross_source_identity()}} can be truncated away simply because a
common formula/isobar crowds the same mass window with more than
\code{top_n} \code{compound_db} entries within tolerance - the standards-library
match is exactly the strongest evidence the candidate could have, and it
never gets the chance to compete.

This function undoes that for the (typically very small) subset of
\code{neutral_mass_id} values where \code{standard_candidates} found at least one
hit: it re-runs \code{\link[=match_compound_db_by_mass]{match_compound_db_by_mass()}} for those masses only,
with \code{top_n = NULL} (no cut), and unions the recovered rows into
\code{compound_candidates}, deduplicating on \code{neutral_mass_id} +
\code{compound_identity_id} so a candidate that already survived the
original cut is not duplicated. \code{neutral_mass_id} values with no
standard-library hit are untouched - \code{top_n} still applies to the
overwhelming majority of masses exactly as before, so this has no
effect on runtime or output size outside the small subset where a
standard is actually in play.
}
\keyword{internal}
